name: flutter_facebook_auth_web
description: web support for flutter_facebook_auth
version: 6.1.2
homepage: https://github.com/darwin-morocho/flutter-facebook-auth/tree/master/facebook_auth_web


environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: ">=3.19.2"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  flutter_facebook_auth_platform_interface:
    git:
      url: https://github.com/iandis/flutter-facebook-auth.git
      path: facebook_auth_platform_interface
      ref: flutter-facebook-auth-7.1.4
  web: ^1.1.1
  # flutter_facebook_auth_platform_interface:
    # path: ../facebook_auth_platform_interface
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      web:
        pluginClass: FlutterFacebookAuthPlugin
        fileName: flutter_facebook_auth_web.dart
